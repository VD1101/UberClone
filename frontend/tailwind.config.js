module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Includes all JS, JSX, TS, and TSX files in the `src` directory
    './public/index.html',       // Include your HTML file if you're using it
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
